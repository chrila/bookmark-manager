<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Public:</strong>
  <%= @category.public %>
</p>

<p>
  <strong>Parent Category:</strong>
  <%= @category.parent_category ? link_to(@category.parent_category, @category.parent_category) : '(none)' %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
