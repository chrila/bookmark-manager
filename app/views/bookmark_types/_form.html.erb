<%= form_with(model: bookmark_type) do |form| %>
  <% if bookmark_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookmark_type.errors.count, "error") %> prohibited this bookmark_type from being saved:</h2>

      <ul>
        <% bookmark_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
